'use strict';

let hasDriversLiscense = false;
const passTest = true;

if (passTest) hasDriverLiscense = true; // <- Strict mode will indicate to this line
if (hasDriversLiscense) console.log('I can drive :D');

const interface = 'Audio'; // <- Unexpected strict mode reserved wordUnderstand this error

